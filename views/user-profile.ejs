<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
    <title> <% title %> </title>
  </head>
  <body>
    <h1> <%= user.nickname %> <%= title %> </h1>
    <a href='/'>Home</a>
    <div class="uiScrollableAreaBody">
        <div class="card">
          <img src="../coffee.png" alt="coffee" style="width:5%">
          <div><p>This is the content of <%= req.user %></p></div>
		  <p>NOTE: <%= _raw %> and <%= _json %> properties have been ommited</p>
        </div>
		<%= userProfile %>
    </div>
